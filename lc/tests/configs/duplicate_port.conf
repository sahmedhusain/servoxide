# Two servers claim the exact same host:port:server_name -> must be detected.
server {
    host 127.0.0.1;
    port 8090;
    server_name dup;
    route / { methods GET; root www; }
}
server {
    host 127.0.0.1;
    port 8090;
    server_name dup;
    route / { methods GET; root www; }
}
